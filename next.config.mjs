/** @type {import('next').NextConfig} */
const nextConfig = {
  distDir: "dist", // Custom output folder
//   output: "standalone", // Enable standalone mode for serverless deployment
};

export default nextConfig;

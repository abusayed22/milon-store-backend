/** @type {import('next').NextConfig} */
const nextConfig = {
  // distDir: "dist", // Custom output folder  -----it's not working when deploy on vercel
//   output: "standalone", // Enable standalone mode for serverless deployment
};

export default nextConfig;
